@model MovieApp.Models.Genrie

@{
    ViewData["Title"] = "Create";
    Layout = "~/Views/AdminPanel/_AdminPanelLayout.cshtml";
}

<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="GenrieName" class="control-label">Genrie Name</label>
                <input asp-for="GenrieName" class="form-control" />
                <span asp-validation-for="GenrieName" class="text-danger"></span>
            </div>
            <div class="row">
                <div class="form-group col-6">
                    <button type="submit" class="btn btn-success py-2 px-3"> <i class="bi bi-check2"></i> Save</button>
                </div>
                <div class="form-group col-6">
                    <a asp-action="Index" onclick="window.history.go(-1); return false;"><button class="btn btn-primary"> <i class="bi bi-x"></i> Cancel</button></a>
                </div>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
